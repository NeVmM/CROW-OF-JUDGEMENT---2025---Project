draw_set_color(c_white);

var scale = 0.5; // Text scale: 1 = normal, 2 = double
draw_text_transformed(x, y, "After doing a double jump, hold \n the SPACEBAR to Glide.", scale, scale, 0);

draw_set_font(FontTutorial);